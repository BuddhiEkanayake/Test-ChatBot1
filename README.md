# Test-ChatBot1
Test-ChatBot1 is a simple rule-based chatbot built using Python. It does not use machine learning or AI models. Instead, it relies on basic conditional statements, predefined responses, and simple logic to interact with users.
